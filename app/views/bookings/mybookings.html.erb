<h1 class="text-center">My Bookings.</h1>
<div class="text-center m-3">
  <%= link_to "Back to Books", books_path, class:"btn btn-outline-dark"  %>
</div>
<div class="container justify-content-center d-flex flex-wrap">

  <% @bookings.each_with_index do |booking, index| %>
  <div class="card shadow p-3 mb-5 bg-body rounded show-book">
    <h5><%= index+1 %> || You booked this at: <br> <%= booking.created_at %></h5>
    <br>
    <h5>Book: <%= booking.book.title %></h5>
    <br>
    <p>Owner: <%= booking.book.user.name %></p>
    <p> Wechat: <%= booking.book.user.we_chat  %> </p>
    <br>
    <p>Period: <%= booking.starting_date %> to <%= booking.ending_date %></p>
  </div>
  <% end %>
</div>



